<h3 class="mb-3">Invite a friend to earn credits.</h3>
<p>
  Earn <b>1</b> promotion credit for every friend you invite who applies to The Teen Magazine.
</p>
<%= form_for invitation, :remote=>true do |f| %>
  <% if invitation.errors.any? %>
    <h6 class="mt-3 text-danger"><%= pluralize(invitation.errors.count, "error")%> prevented this invitation from sending:</h6>
    <ul>
      <% invitation.errors.full_messages.each do |msg| %>
      <li class="text-danger"><h6><%= msg %></h6></li>
      <% end %>
    </ul>
  <% end %>
  <div class="pitch_flds mb-3 mt-4 pl-0">
    <%= f.text_field :email, placeholder: "Enter Email Address", onkeyup: "isFormButtonDisabled('#invitation_email')" %>
    <%= f.hidden_field :status, value: "Not viewed" %>
    <%= f.hidden_field :token, value: [*('A'..'Z'),*('a'..'z')].shuffle[0,31].join %>
    <div class="btn_cntnr mt-0 mb-0 pb-0 pt-2 pl-0" style="float: none; position: relative;">
      <div class="spinner-button spinner-border hide" style="margin-left: 38%;" role="status">
        <span class="sr-only">Loading...</span>
      </div>
      <%= f.button "Send Invite!", class: "newpost--new--profile mr-0 pl-4 pr-4 mb-0 submit_modal disabled", id: "submit-comment-modal-button" %>
    </div>
  </div>
  <small>
    When you click <i>Send Invite</i>, we will send an email inviting your friend(s) to apply to The Teen Magazine. The email will include a special apply link from you that will waive the cover letter and prioritize their application for review.
    The referral link will expire in 1 week so they must apply before then to benefit from the easier application. For you to earn a promotional credit, your friend must apply to The Teen Magazine using your referral link.
  </small>
<% end %>

<script type="text/javascript">
  $(document).ready(function() {
    isFormButtonDisabled('#invitation_email');
  });
</script>
